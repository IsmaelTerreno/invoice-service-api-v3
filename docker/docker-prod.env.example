# =====================================================================
# Docker Production Environment Variables
# =====================================================================
# This file contains all environment variables needed for production
# Docker build and deployment.
#
# INSTRUCTIONS:
# 1. Copy this file to docker-prod.env
# 2. Fill in all the values with your production credentials
# 3. NEVER commit docker-prod.env to version control
#
# cp docker-prod.env.example docker-prod.env
# =====================================================================

# ------------- Server Configuration-------------
# Port where the application will listen
SERVER_PORT_LISTENING=3120

# CORS allowed origins (comma-separated, or use * for all)
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOW_ORIGINS=https://your-production-domain.com
# ------------- Server Configuration-------------

# ------------- Database Configuration-------------
# PostgreSQL database host (IP or domain name)
HOST_DB_CONFIG=your-db-host.example.com

# PostgreSQL database port
PORT_DB_CONFIG=5432

# Database name
DATABASE_NAME_DB_CONFIG=remotejob

# Database username
USER_NAME_DB_CONFIG=your_db_user

# Database password (use strong password)
USER_PASSWORD_DB_CONFIG=your_secure_db_password
# ------------- Database Configuration-------------

# ------------- JWT Provider Configuration-------------
# URL endpoint for JWT authentication service
JWT_URL_ENDPOINT=https://your-auth-service.example.com/api/v1/user

# JWT Access Token Secret (base64 encoded, min 256 bits)
# Generate with: openssl rand -base64 64
JWT_SECRET_ACCESS=your_base64_encoded_jwt_access_secret_here

# JWT Refresh Token Secret (base64 encoded, min 256 bits)
# Generate with: openssl rand -base64 64
JWT_SECRET_REFRESH=your_base64_encoded_jwt_refresh_secret_here
# ------------- JWT Provider Configuration-------------

# ------------- Test E2E Configuration (Optional)-------------
# These are used for E2E testing in production-like environments
TEST_USER_NAME=testuser
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=testpassword
# ------------- Test E2E Configuration-------------

# ------------- RabbitMQ Configuration-------------
# RabbitMQ host (IP or domain name)
RABBITMQ_HOST=your-rabbitmq-host.example.com

# RabbitMQ port
RABBITMQ_PORT=5672

# RabbitMQ username
RABBITMQ_USERNAME=your_rabbitmq_user

# RabbitMQ password (use strong password)
RABBITMQ_PASSWORD=your_secure_rabbitmq_password

# Application queue names
INVOICE_STATUS_ON_RELATED_PLANS_RABBITMQ_QUEUE_NAME=invoice-status-on-related-plans
NOTIFICATION_EVENTS_RABBITMQ_QUEUE_NAME=notification-events
# ------------- RabbitMQ Configuration-------------

# ------------- Stripe Configuration-------------
# Stripe Secret Key (production key starts with sk_live_)
# WARNING: Use production keys only in production environment
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key_here

# Stripe Webhook Secret (starts with whsec_)
# Get this from Stripe Dashboard -> Developers -> Webhooks
STRIPE_ENDPOINT_SECRET=whsec_your_stripe_webhook_secret_here
# ------------- Stripe Configuration-------------

# =====================================================================
# SECURITY NOTES:
# =====================================================================
# - NEVER commit docker-prod.env with real credentials to git
# - Use strong, unique passwords for all credentials
# - Rotate secrets regularly
# - Use Stripe production keys (sk_live_*) only in production
# - Keep JWT secrets at least 256 bits (64 base64 characters)
# - Restrict CORS origins to your actual domains (avoid using *)
# =====================================================================
